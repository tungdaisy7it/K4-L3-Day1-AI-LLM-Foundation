# KHÔNG BAO GIỜ commit API key
.env

# Tài liệu nội bộ của giảng viên — không phát cho học viên
docs/

.venv/
venv/
__pycache__/
.pytest_cache/
solution.zip

# Cấu hình cục bộ của Claude Code — không cần thiết cho bài nộp
.claude/
